language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm run coverage
  - npm run report-coverage
  - npm run build
deploy:
  - provider: npm
    skip_cleanup: true
    email: $NPM_EMAIL
    api_key: $NPM_TOKEN
    on:
      tags: true
      branch: master

